Used os library methods to check if the file ends with particualr format. Used the same to confirm if the given path is a directory or a file.

Complexity is O(n) where n is the number of directories or Files in the given path